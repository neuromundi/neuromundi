/**
 * toolkit — punto de entrada del contenido del Kit de Herramientas.
 * El contenido por idioma vive en ./toolkitContent (generado). Usa getModules()
 * con el idioma activo; si no hay ese idioma, cae en inglés y luego español.
 */
export * from './toolkitContent';
