/**
 * roleFeatures — OBSOLETO.
 *
 * El contenido "esto incluye tu cuenta" se movió a i18n para traducirse a los 8
 * idiomas: claves `roleFeatures.<tipo>.title` y `roleFeatures.<tipo>.features`
 * (arreglo) en `src/i18n/locales/*.json`. Lo consume `RoleFeaturesPanel`.
 *
 * Este archivo se conserva vacío solo porque el disco montado no permite
 * borrarlo; puede eliminarse desde Windows.
 */
export {};
