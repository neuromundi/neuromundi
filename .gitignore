# ── Dependencias ──────────────────────────────────────────────
node_modules/

# ── Build / salida ────────────────────────────────────────────
dist/
dist-ssr/
neuromundi-dist-v3-mapa/
*.local

# ── Caché y temporales (se regeneran) ─────────────────────────
*.tsbuildinfo
*.timestamp-*.mjs
.vite/

# ── Entorno / secretos (NO versionar) ─────────────────────────
.env
.env.*
!.env.example

# ── Archivo y respaldos (no versionar) ────────────────────────
_archivo/
backups/

# ── Editor / Sistema operativo ────────────────────────────────
.DS_Store
Thumbs.db
.vscode/
.idea/

# ── Logs ──────────────────────────────────────────────────────
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

dist-app/
*.zip

# Cualquier archivo de variables, no solo el llamado .env
*.env

# Datos operativos y scratch
Claude outputs/
marketing/video/ssml/
*.csv
residencias_*.json
sector62_clases.json
avance_residencias.json
PARCHE-*.md

# Cache local de skills/plugins
.claude/

# Basura de Windows y mockups de revisión (no van al repo)
desktop.ini
**/desktop.ini
mockup*.html
